import type { Metadata } from "next";
//! Wordle solver 
// TODO: take all the possible wordle words then get the "best word combo" maybe write a python script that auto does that or an api then call the api or something like that
export default async function Page() {
  return (
    <div>
        <h1>Wordle solver</h1>

    </div>
  );
}
export const metadata: Metadata = {
  title: "Wordle solver - wiktrek.xyz",
  description: "Wordle solver",
};

